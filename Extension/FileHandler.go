package Extension

type IOFileHandler struct {
	//
}

func (*IOFileHandler) retFilePtr(dirpath string, filename string) {
	//pdirname := filepath.Dir(dirpath)
	//pfilename := filepath.Base(filename)
}
